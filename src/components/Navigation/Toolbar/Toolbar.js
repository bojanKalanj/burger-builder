import React from 'react';

const toolbar = (props) => (
    <header className='Toolbar'>
        <dic>Menu</dic>
        <dic>Logo</dic>
        <nav>
            ...
        </nav>
    </header>
);

export default toolbar;